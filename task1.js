/* Rozwiązanie z użuciem rekurencji */

function recursion() {
    recursion();
  }
  
  recursion();

/* Po uruchomieniu tego kodu, konsola wyświetli komunikat błędu: Uncaught RangeError: Maximum call stack size exceeded */